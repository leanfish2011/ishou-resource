部署说明：
1、服务器安装好ubuntu操作系统
2、服务器安装好docker。命令：sudo apt-get update && sudo apt install docker.io
3、拷贝ishou_deploy.sh、镜像tar包到服务器（镜像tar包较大，另外存储）
4、服务器上执行./ishou_deploy.sh
5、http://ip访问系统
6、http://ip:9000访问镜像管理界面